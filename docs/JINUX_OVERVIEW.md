# Jinux 操作系统

**Jinux** - 用 Java 实现的 Linux 0.01 版本模拟操作系统

## 项目简介

Jinux 是一个教学用操作系统，尽可能还原了 Linux 0.01 的核心架构和机制。该项目专注于操作系统核心概念的实现，包括进程管理、内存管理、设备驱动、系统调用等。

## 系统架构

### 模块划分（参照 Linux 0.01）

```
Jinux/
├── src/main/java/jinux/
│   ├── boot/           # 启动引导
│   │   └── Bootstrap.java
│   ├── kernel/         # 核心模块
│   │   ├── Kernel.java
│   │   ├── Task.java
│   │   ├── Scheduler.java
│   │   ├── Signal.java              # 信号机制
│   │   └── SystemCallDispatcher.java
│   ├── mm/             # 内存管理
│   │   ├── MemoryManager.java
│   │   ├── PhysicalMemory.java
│   │   ├── AddressSpace.java
│   │   └── PageTable.java
│   ├── fs/             # 文件系统
│   │   ├── VirtualFileSystem.java
│   │   ├── Inode.java               # 索引节点
│   │   ├── SuperBlock.java          # 超级块
│   │   ├── BufferCache.java         # 缓冲区缓存
│   │   ├── File.java
│   │   └── FileDescriptorTable.java
│   ├── ipc/            # 进程间通信
│   │   ├── Pipe.java                # 管道实现
│   │   └── PipeFile.java            # 管道文件封装
│   ├── shell/          # Shell 命令解释器
│   │   └── SimpleShell.java         # 简单 Shell 实现
│   ├── drivers/        # 设备驱动
│   │   ├── Device.java
│   │   ├── CharDevice.java
│   │   ├── BlockDevice.java
│   │   ├── ConsoleDevice.java
│   │   ├── VirtualDiskDevice.java
│   │   └── ClockDevice.java
│   ├── lib/            # 用户态库
│   │   ├── LibC.java                # C 库封装
│   │   ├── LibCExample.java
│   │   ├── SignalExample.java       # 信号演示
│   │   └── PipeExample.java         # 管道演示
│   ├── include/        # 公共定义
│   │   ├── Const.java
│   │   ├── Types.java
│   │   └── Syscalls.java
│   └── init/           # 初始化和用户程序
│       └── InitProcess.java
├── pom.xml             # Maven 配置
└── README.md           # 项目文档
```

## 已实现的核心功能

### 1. 内存管理（mm/）

- **物理内存管理**：位图方式管理 16MB 物理内存
- **虚拟地址空间**：每个进程独立的虚拟地址空间（64MB）
- **页表机制**：虚拟地址到物理地址的映射
- **页面分配**：动态页面分配和释放
- **堆管理**：brk 系统调用支持

### 2. 进程管理（kernel/）

- **进程控制块（Task）**：类似 Linux task_struct
- **进程调度器**：时间片轮转 + 优先级调度算法
- **进程状态管理**：RUNNING, INTERRUPTIBLE, UNINTERRUPTIBLE, ZOMBIE, STOPPED
- **时间片管理**：动态时间片分配
- **进程表**：最多支持 64 个进程

### 3. 系统调用（kernel/）

实现的系统调用（共 22+ 个）：

**进程管理**
- `fork()` - 创建子进程
- `exit()` - 退出进程
- `wait()` - 等待子进程
- `getpid()` - 获取进程 ID
- `getppid()` - 获取父进程 ID
- `pause()` - 暂停进程直到收到信号

**信号管理**
- `signal()` - 设置信号处理器
- `kill()` - 发送信号到进程

**文件操作**
- `read()` - 读取文件
- `write()` - 写入文件
- `open()` - 打开文件
- `close()` - 关闭文件
- `lseek()` - 定位文件指针
- `creat()` - 创建文件
- `unlink()` - 删除文件

**目录操作**
- `chdir()` - 改变当前目录
- `mkdir()` - 创建目录
- `rmdir()` - 删除目录

**进程间通信**
- `pipe()` - 创建管道

**内存管理**
- `brk()` - 设置堆结束地址

**时间**
- `time()` - 获取系统时间

**其他**
- `sync()` - 同步磁盘缓冲区

### 4. 设备驱动（drivers/）

- **控制台设备**：标准输入输出
- **虚拟磁盘设备**：文件模拟的块设备（10MB）
- **时钟设备**：定时器中断（100 HZ）

### 5. 信号机制（kernel/Signal.java）

- **标准信号**：支持 32 种 Unix 标准信号
  - 进程控制：SIGTERM, SIGKILL, SIGSTOP, SIGCONT
  - 异常信号：SIGSEGV, SIGILL, SIGFPE
  - 用户信号：SIGINT, SIGQUIT, SIGHUP
  - 子进程：SIGCHLD
  - 管道：SIGPIPE
- **信号处理**：支持 SIG_DFL（默认）、SIG_IGN（忽略）、自定义处理器
- **信号保护**：SIGKILL 和 SIGSTOP 不可被捕获或忽略
- **信号位图**：待处理信号和屏蔽信号的位图管理
- **默认行为**：终止、忽略、停止、核心转储

### 6. 文件系统（fs/）

- **VFS 层**：虚拟文件系统抽象
- **Inode**：索引节点，支持文件/目录/设备类型
  - 直接块指针（10个）
  - 一级间接块指针
  - 二级间接块指针
- **SuperBlock**：超级块管理文件系统元数据
  - Inode 位图和数据块位图
  - 根文件系统：1024 inodes, 10240 blocks
- **BufferCache**：块设备缓冲区缓存
  - LRU 缓存策略
  - 脏块管理
- **文件对象**：打开文件的内存表示
  - 文件位置指针
  - 引用计数管理

### 7. 管道机制（ipc/Pipe.java）

- **循环缓冲区**：4KB 大小的环形缓冲区
- **阻塞式 I/O**：读写操作支持阻塞等待
- **引用计数**：读端和写端分别计数
- **EOF 检测**：写端关闭时，读操作返回 0
- **SIGPIPE 信号**：读端关闭时，写操作触发 SIGPIPE
- **系统调用**：`pipe()` 创建管道对

### 8. 用户态库（lib/LibC.java）

- **进程管理**：getpid, fork, exit, waitpid, pause
- **文件 I/O**：open, close, read, write, lseek, creat, unlink
- **目录操作**：chdir, mkdir, rmdir
- **信号处理**：signal, kill
- **进程间通信**：pipe
- **内存管理**：brk, malloc（简化版）
- **时间**：time
- **其他**：sync, print, println

### 9. Shell 命令解释器（shell/SimpleShell.java）

- **交互式界面**：命令行提示符 `jinux$`
- **内置命令**：help, ps, mem, time, signal, kill, demo, echo, clear, uptime, version, exit
- **系统信息查询**：进程列表、内存统计、系统时间
- **信号管理**：发送信号到指定进程
- **演示功能**：运行信号、管道、LibC 演示
- **命令解析**：简单的空格分割解析

### 10. 调度和中断

- **时钟中断**：每 10ms 触发一次
- **进程调度**：基于时间片和优先级的调度算法
- **上下文切换**：进程切换机制
- **信号处理**：调度时检查并处理待处理信号

## 编译和运行

### 环境要求

- Java JDK 17 或更高版本
- Maven 3.x

### 编译（Maven 方式）

```bash
cd Jinux
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home
mvn clean compile
```

### 运行

```bash
mvn exec:java
```

### 打包

```bash
mvn package
java -jar target/jinux-0.0.1-SNAPSHOT.jar
```

### 运行示例输出

```
     _ _                  
    | (_)                 
    | |_ _ __  _   ___  __
 _  | | | '_ \| | | \ \/ /
| |_| | | | | | |_| |>  < 
 \___/|_|_| |_|\__,_/_/\_\

Jinux - A Java Implementation of Linux 0.01
Version 0.01-alpha

========================================
        Jinux Operating System
    (Java Implementation of Linux 0.01)
========================================

[MM] Physical memory initialized: 16MB, 3840 pages free
[KERNEL] Starting kernel initialization...

[CONSOLE] Console device initialized
[DISK] Virtual disk initialized: 10MB
[CLOCK] Clock device initialized: 100 HZ
[VFS] Initializing virtual file system...
[FS] Initialized filesystem: 1024 inodes, 10240 zones
[VFS] Created root directory

[KERNEL] Creating init process (PID 0)...
[KERNEL] Starting Jinux...

[INIT] Init process started!
[INIT] Jinux operating system is now running.

========== LibC 使用示例 ==========
[LibC] getpid() = 0
[LibC] malloc(4096) = 0x0
[LibC] time() = 1766420167
====================================

========== Signal Mechanism Demo ==========
[DEMO] Setting SIGINT handler to SIG_IGN...
[DEMO] Sending SIGINT (should be ignored)...
[SIGNAL] Ignored SIGINT
[DEMO] Process still running
===========================================

========== Pipe Mechanism Demo ==========
[DEMO] Creating a pipe...
[DEMO] Wrote 16 bytes: "Hello from pipe!"
[DEMO] Read 16 bytes: "Hello from pipe!"
[DEMO] Pipe capacity: 4096 bytes
=========================================

[INIT] Entering main loop...
```

## 核心数据结构

### Task（进程控制块）

```java
- pid：进程 ID
- ppid：父进程 ID
- state：进程状态
- counter：时间片计数器
- priority：优先级
- addressSpace：地址空间
- fdTable：文件描述符表
- signalPending：待处理信号位图
- signalBlocked：信号屏蔽位图
- signalHandlers：信号处理器数组（32个）
```

### AddressSpace（地址空间）

```java
- pageTable：页表
- codeStart/End：代码段范围
- dataStart/End：数据段范围
- brk：堆结束地址
- stackTop：栈顶地址
```

### PageTable（页表）

```java
- mappings：虚拟页 -> 物理页映射
- flags：页面权限标志
```

## 与 Linux 0.01 的对应关系

| Linux 0.01 | Jinux | 完成度 |
|------------|-------|--------|
| boot/bootsect.s | boot/Bootstrap.java | ✅ 100% |
| kernel/sched.c | kernel/Scheduler.java | ✅ 95% |
| kernel/signal.c | kernel/Signal.java | ✅ 90% |
| kernel/sys_call.s | kernel/SystemCallDispatcher.java | ✅ 90% |
| mm/memory.c | mm/MemoryManager.java | ✅ 90% |
| fs/ | fs/ | ✅ 65% |
| fs/pipe.c | ipc/Pipe.java | ✅ 85% |
| lib/ | lib/LibC.java | ✅ 90% |
| include/linux/*.h | include/*.java | ✅ 100% |
| drivers/ | drivers/ | ✅ 75% |

## 设计特点

### 优势

1. **类型安全**：Java 语言避免了指针错误和内存泄漏
2. **易于理解**：面向对象设计清晰表达了 OS 概念
3. **跨平台**：可在任何支持 JVM 的平台运行
4. **易于调试**：可使用 IDE 调试工具

### 局限性

1. **不是真实内核**：运行在 JVM 之上，无法直接控制硬件
2. **性能开销**：多了 JVM 和宿主 OS 的层次
3. **无法实现某些底层机制**：如真实的页表、中断等

## 核心概念演示

系统启动后会自动演示以下功能：

1. **系统调用演示**：getpid, getppid, time, write, brk
2. **进程管理演示**：进程状态、优先级、时间片
3. **内存管理演示**：地址空间布局、堆分配

## 技术亮点

1. **完整的进程模型**：包括 PCB、调度、上下文切换、信号处理
2. **虚拟内存机制**：页表、地址转换、页面分配
3. **系统调用框架**：分发器、处理器映射（22+ 系统调用）
4. **信号机制**：32 种信号、信号处理器、默认行为
5. **文件系统**：VFS、Inode、SuperBlock、缓冲区缓存
6. **进程间通信**：管道机制、阻塞式 I/O
7. **设备驱动抽象**：字符设备、块设备
8. **时钟中断模拟**：定时器驱动的调度
9. **用户态库**：LibC 封装 35+ 函数

## 扩展方向

已完成：
- ✅ 信号机制（signal, kill 系统调用）
- ✅ 管道 IPC（pipe 系统调用）
- ✅ 文件系统框架（VFS, Inode, SuperBlock）
- ✅ 用户态库（LibC 封装）
- ✅ **Shell 命令解释器**（交互式界面）🎉

未来可以实现的功能：

1. **execve 系统调用**：加载和执行新程序
2. **完整的文件读写**：实际的块设备 I/O
3. **Shell 增强功能**：命令管道 `|`、文件重定向 `>` `<`、后台执行 `&`
4. **多进程演示**：fork 和 pipe 结合的实际应用
5. **更多 IPC 机制**：消息队列、共享内存

## 学习价值

Jinux 适合用于：

- 理解操作系统核心概念
- 学习进程调度算法
- 理解虚拟内存机制
- 学习系统调用实现
- 理解信号机制和 IPC
- 理解文件系统架构
- 操作系统课程实验
- Java 系统编程实践

## 代码统计

- **总文件数**：33 个 Java 文件
- **代码行数**：6044 行（含注释）
- **注释率**：约 35%
- **模块数**：9 个主要模块
- **系统调用**：22+ 个
- **信号支持**：32 种
- **Shell 命令**：12 个
- **打包大小**：约 70 KB

## 许可证

本项目为教学演示项目。

## 致谢

本项目参考了 Linux 0.01 的设计和实现。

---

**Jinux** - 用 Java 理解 Linux
